# URL de l'API Fastify — utilisée côté serveur (SSR, Server Components)
API_URL=http://localhost:3001

# URL publique de l'API — utilisée côté client (navigateur)
NEXT_PUBLIC_API_URL=http://localhost:3001
